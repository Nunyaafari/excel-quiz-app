import { ImageResponse } from 'next/og'
import { decodeShareResultsToken } from '@/lib/share-results'

export const alt = 'Excel Mastery Quiz share badge'
export const runtime = 'edge'
export const dynamic = 'force-dynamic'
export const size = {
  width: 1200,
  height: 630,
}
export const contentType = 'image/png'

type OpenGraphImageProps = {
  params: Promise<{
    token: string
  }>
}

export default async function OpenGraphImage({ params }: OpenGraphImageProps) {
  const { token } = await params
  const snapshot = decodeShareResultsToken(token)

  const percentage = snapshot?.percentage ?? 0
  const performanceLabel = snapshot?.performanceLabel ?? 'Performance Summary'
  const correctAnswers = snapshot?.correctAnswers ?? 0
  const totalQuestions = snapshot?.totalQuestions ?? 0
  const profileLabel = snapshot?.profileLabel ?? 'Mixed Difficulty'
  const completedLabel = snapshot?.completedLabel ?? 'Recently'
  const scoreColor = percentage >= 80 ? '#16a34a' : percentage >= 60 ? '#d97706' : '#dc2626'

  return new ImageResponse(
    (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'flex',
          background: 'linear-gradient(135deg, #0f2744 0%, #144d6a 55%, #1f6f6d 100%)',
          color: 'white',
          fontFamily: 'Arial, sans-serif',
          padding: 36,
          boxSizing: 'border-box',
          position: 'relative',
        }}
      >
        <div
          style={{
            position: 'absolute',
            right: -42,
            top: -56,
            width: 228,
            height: 228,
            borderRadius: 9999,
            background: 'rgba(93, 214, 207, 0.18)',
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: -56,
            bottom: -74,
            width: 250,
            height: 250,
            borderRadius: 9999,
            background: 'rgba(143, 184, 255, 0.14)',
          }}
        />
        <div
          style={{
            width: '100%',
            height: '100%',
            borderRadius: 30,
            border: '1px solid rgba(255,255,255,0.18)',
            background: 'rgba(255,255,255,0.08)',
            display: 'flex',
            padding: 38,
            justifyContent: 'space-between',
            boxSizing: 'border-box',
            position: 'relative',
          }}
        >
          <div
            style={{
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'flex-start',
              width: 700,
              paddingRight: 28,
              boxSizing: 'border-box',
            }}
          >
            <div style={{ marginTop: 6, fontSize: 58, fontWeight: 700, lineHeight: 1.08 }}>Excel Quiz Result</div>
            <div style={{ marginTop: 18, fontSize: 28, color: '#D6EBFF', lineHeight: 1.35 }}>
              {`${performanceLabel} on the Excel Mastery Quiz`}
            </div>
            <div style={{ marginTop: 18, fontSize: 24, color: '#D6EBFF', lineHeight: 1.4 }}>
              Shareable performance snapshot
            </div>

            <div
              style={{
                marginTop: 28,
                display: 'flex',
                flexWrap: 'wrap',
                gap: 12,
              }}
            >
              <div
                style={{
                  borderRadius: 9999,
                  border: '1px solid rgba(255,255,255,0.22)',
                  background: 'rgba(255,255,255,0.08)',
                  padding: '12px 18px',
                  fontSize: 22,
                  color: '#D6EBFF',
                }}
              >
                {`Track: ${profileLabel}`}
              </div>
              <div
                style={{
                  borderRadius: 9999,
                  border: '1px solid rgba(255,255,255,0.22)',
                  background: 'rgba(255,255,255,0.08)',
                  padding: '12px 18px',
                  fontSize: 22,
                  color: '#D6EBFF',
                }}
              >
                {`Completed: ${completedLabel}`}
              </div>
            </div>

            <div style={{ marginTop: 'auto', display: 'flex', flexDirection: 'column', gap: 8 }}>
              <div style={{ fontSize: 24, color: '#D6EBFF' }}>Generated by Excel Mastery Quiz</div>
              <div style={{ fontSize: 22, fontWeight: 700, letterSpacing: 1.4, color: '#C8E8FF' }}>
                Animeight Training Institute
              </div>
            </div>
          </div>

          <div style={{ display: 'flex', flexDirection: 'column', width: 310, alignItems: 'flex-end' }}>
            <div
              style={{
                width: '100%',
                display: 'flex',
                flexDirection: 'column',
                background: 'white',
                borderRadius: 28,
                padding: '30px 28px',
                boxSizing: 'border-box',
              }}
            >
              <div style={{ fontSize: 92, fontWeight: 700, color: scoreColor, lineHeight: 1 }}>{`${percentage}%`}</div>
              <div style={{ marginTop: 10, fontSize: 32, fontWeight: 700, color: '#183251' }}>{performanceLabel}</div>
              <div style={{ marginTop: 14, fontSize: 24, color: '#5A6F8A' }}>{`${correctAnswers}/${totalQuestions} correct`}</div>
            </div>

            <div
              style={{
                marginTop: 18,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'flex-end',
                gap: 12,
                width: '100%',
                color: '#D6EBFF',
                fontSize: 22,
              }}
            >
              <div
                style={{
                  width: 64,
                  height: 64,
                  borderRadius: 20,
                  background: 'rgba(255,255,255,0.15)',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 28,
                  fontWeight: 700,
                  letterSpacing: 1,
                }}
              >
                XLS
              </div>
              <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-end' }}>
                <div style={{ fontSize: 20, color: '#C8E8FF' }}>Excel Mastery Quiz</div>
                <div style={{ fontSize: 18 }}>Verified score snapshot</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    ),
    {
      ...size,
    }
  )
}
